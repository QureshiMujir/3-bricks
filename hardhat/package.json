{
    "name": "hardhat-project",
    "scripts": {
        "deploy": "npm run hardhat -- run scripts/deploy.js --network amoy"
    },
    "devDependencies": {
        "@nomicfoundation/hardhat-toolbox": "^2.0.1",
        "hardhat": "^2.12.6"
    },
    "dependencies": {
        "@openzeppelin/contracts": "^4.8.1",
        "chai": "^4.3.7",
        "dotenv": "^16.0.3"
    }
}
